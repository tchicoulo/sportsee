import React from "react";

const Performances = ({ data }) => {
  // console.log(data);
  return (
    <div className="performances">
      <h2>performances</h2>
    </div>
  );
};

export default Performances;
